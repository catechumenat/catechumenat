---js
const eleventyNavigation = {
	key: "Home",
	order: 1
};

const numberOfLatestPostsToShow = 10;
---

<h2>Bienvenue au catéchuménat de Saint-Nicolas-des-Champs !</h2>

Vous trouverez le programme des soirées sur la page <a href="/about/">le programme</a>, ainsi que les informations pratiques. Ci-dessous des enregistrements des enseignements passés.

Vous avez des questions ? on a préparé des FAQ ("foires aux questions") pour y voir plus clair : <a href="/faq/faq-du-bapteme/">sur la préparation au baptême</a>, <a href="/faq/faq-du-bapteme-par-immersion/">sur le baptême par immersion</a>, et <a href="/faq/faq-de-la-confirmation/">sur la préparation à la confirmation</a>.

Vous souhaitez vous inscrire ? <a href="https://public.enoria.app/pubinsc/catechumenat/1044/NjI1NzY1MzY0MjY">Remplissez ce formulaire</a>, et on revient vers vous.

{% set postsCount = collections.posts | length %}
{% set latestPostsCount = postsCount | min(numberOfLatestPostsToShow) %}
<h1>Les {{ latestPostsCount }} derniers enseignements</h1>

{% set postslist = collections.posts | head(-1 * numberOfLatestPostsToShow) %}
{% set postslistCounter = postsCount %}
{% include "postslist.njk" %}

{% set morePosts = postsCount - numberOfLatestPostsToShow %}
{% if morePosts > 0 %}
<p>{{ morePosts }} more post{% if morePosts != 1 %}s{% endif %} can be found in <a href="blog.njk">the archive</a>.</p>
{% endif %}

{# List every content page in the project #}
{#
<ul>
	{%- for entry in collections.all %}
	<li><a href="{{ entry.url }}"><code>{{ entry.url }}</code></a></li>
	{%- endfor %}
</ul>
#}
